version: '3.8'

services:
  pipeline:
    build: .
    working_dir: /app
    volumes:
      # Syncs your local project folder to the container
      - .:/app
    ports:
      # Exposes Jupyter Notebook to your local browser
      - "8888:8888"
